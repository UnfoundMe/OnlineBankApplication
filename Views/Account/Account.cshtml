@model Account

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Account Name</th>
            <th>Balance</th>            
        </tr>
    </thead>
    <tbody>       
            <tr>
                <td>@Model.AccountName</td>
                <td>@Model.Balance</td>                
            </tr>
        <a href="@Url.Action("AccountList", "Account")" class="btn btn-sm btn-secondary">Back</a>
    </tbody>
</table>